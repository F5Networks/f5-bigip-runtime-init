# Examples

## Simple

Simple example showing tool usage.

- Prereqs
    - Terraform
    - Logged into Azure (can use `az login`)
- Deploy
    - cd `examples/simple`
    - `terraform init`
    - `terraform apply`